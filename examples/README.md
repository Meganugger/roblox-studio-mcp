# Examples

- [`client-configs/`](client-configs/) — ready-to-paste MCP client configurations
  (Claude Desktop, Claude Code, Cursor, and URL-only platforms via `remote-url.md`)
- [`prompts.md`](prompts.md) — prompt playbook: effective requests per role
  (level design, systems, UI, testing)
- [`simulator-game.md`](simulator-game.md) — the full acceptance scenario: an agent
  builds a polished simulator game end to end, including the debug loop
